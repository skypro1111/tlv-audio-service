// Package protocol implements TLV packet parsing and validation.
// It handles the binary protocol specified in section 3, including header parsing,
// signaling payload extraction, and audio payload processing.
package protocol 