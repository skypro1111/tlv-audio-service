// Package server implements the UDP server for receiving TLV packets and HTTP API endpoints.
// It handles concurrent packet processing, routing to stream sessions, and provides
// monitoring/management endpoints as specified in section 10.
package server 